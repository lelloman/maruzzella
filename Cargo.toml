[package]
name = "maruzzella"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = { package = "gtk4", version = "0.9" }
glib = "0.20"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
