# Maruzzella

Anonymous GTK desktop shell extracted from the Tufino workbench effort.

Current first-pass scope:
- custom workbench tabs
- custom tool-window tabs
- custom drag/reorder interactions
- custom workbench split previews
- neutral placeholder content instead of product-specific runtime wiring

Run:

```bash
cargo run
```
